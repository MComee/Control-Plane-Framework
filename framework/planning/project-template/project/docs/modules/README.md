# Modules

Create one file per major workstream, capability area, or vision-derived component.

Each module should explain:

* what the module is
* why it matters
* how it relates to the core vision
* which tasks belong to it
* major dependencies
* completion characteristics
