name: Contract Validation V4

on:
  push:
  pull_request:

jobs:
  contract-v4:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: pip install pyyaml

      - name: Run framework validator v4
        run: python scripts/validate_framework_v4.py
